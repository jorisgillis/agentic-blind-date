services:
  app:
    build: .
    container_name: agentic-blind-date
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - ./data:/data
    env_file:
      - path: .env
        required: false
    environment:
      - DB_PATH=/data/blind_date.db
