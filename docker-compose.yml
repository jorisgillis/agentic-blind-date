services:
  app:
    build: .
    container_name: agentic-blind-date
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - ./data:/data
    environment:
      - MISTRAL_API_KEY=${MISTRAL_API_KEY}
      - GITHUB_TOKEN=${GITHUB_TOKEN}
      - DB_PATH=/data/blind_date.db
